<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http//mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yang.repository.UserRepository">
    <select id="findAll" resultType="User"><!--这里的resultType直接写User即可，我们先前配置了扫描javabean包的路径。简化开发-->
        select * from user
    </select>
</mapper>